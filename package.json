{
  "name": "douyin-data-assistant",
  "private": true,
  "version": "0.7.0",
  "description": "Chrome extension and Codex handoff protocol for Douyin research data",
  "type": "module",
  "scripts": {
    "test": "node --test tests/protocol.test.mjs",
    "check": "node --check background.js && node --check popup.js && node --check offscreen.js && node --check protocol.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
