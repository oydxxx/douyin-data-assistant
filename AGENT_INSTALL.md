# 抖音数据助手：给 Agent 的安装与验收指令

## 目标

将本仓库配置为一套可供新手使用的本地工作流：浏览器扩展采集用户已登录抖音数据页面的数据；用户说“导入飞书”后，再通过本机 `feishu-cli` 写入用户指定的已有飞书多维表格。

不要创建自建服务、不要让浏览器扩展保存飞书密钥、不要要求用户手动导出或上传 JSON。

## 授权边界

- 可以检查本机文件、安装本仓库内的 Codex Skill、检查 `feishu-cli` 是否安装及登录。
- 不要在用户没有明确说“导入飞书”前创建、修改或写入飞书数据。
- 不要读取浏览器 Cookie、密码、个人资料或其他站点数据。
- 加载解压扩展是浏览器安装动作；将页面打开并准备好后，让用户确认最后的浏览器安装提示。

## 输入

用户会提供本仓库本地路径或 GitHub 地址。将仓库根目录记为 `REPO`。

## 安装步骤

1. 检查 `REPO/manifest.json`、`REPO/background.js`、`REPO/douyin-import-feishu/SKILL.md` 是否存在；缺失时停止并报告。
2. 将 `REPO/douyin-import-feishu` 安装到当前 Codex Skills 目录。优先使用当前环境的 Skill 安装机制；如果需要复制文件，目标为 `${CODEX_HOME:-~/.codex}/skills/douyin-import-feishu`。
3. 验证 Skill 的 YAML 头部名称为 `douyin-import-feishu`。不要覆盖一个同名但内容不同的用户 Skill；先报告差异。
4. 检查 `feishu-cli` 是否可执行，再检查授权状态。若未安装或未登录，说明缺少前置条件并引导用户完成官方登录；不要索要 App Secret、Access Token 或浏览器 Cookie。
5. 打开 Chrome/Edge 扩展管理页，准备加载 `REPO` 作为“已解压的扩展程序”。在浏览器最终确认安装或权限提示前，交由用户确认。
6. 安装完成后，打开用户已登录的 `https://creator.douyin.com/` 页面。确认扩展弹窗显示“正在监听”或在页面操作后出现采集数量。
7. 告诉用户日常只需：在抖音页面正常搜索，然后对 Agent 说“导入飞书”。不要让用户导出、拖拽或填写密钥。

## 导入飞书时的执行规则

当且仅当用户明确说“导入飞书”“写入飞书”或等价指令时执行以下步骤：

1. 自动寻找最新交接文件：优先读取 `~/Downloads/douyin-data-assistant/agent-handoff.json`；若下载目录被修改，则按 JSON 标识 `"format":"douyin-data-assistant/v2"` 查找最新文件。
2. 验证文件存在、格式正确，并报告将要导入的记录数量。
3. 从当前对话或已保存配置读取目标飞书 Base 和数据表。目标不明确时，列出用户可访问的数据表，让用户只选择一次；不要新建表。
4. 读取目标表字段和采集 JSON 的实际结构。根据字段含义映射关键词、关联词、指标、时间范围等有效数据；不要把整段原始 JSON 写进不相符的业务表。
5. 按“主关键词 + 关联词 + 类型 + 时间范围 + 地域”生成业务键，并在写入前分页读取全表。先合并本次数据中相同业务键的记录；表内已有相同业务键时更新已有记录，而不是新增。缺少主关键词或关联词的数据跳过并报告。
6. 只对新业务键使用 `feishu-cli bitable record batch-create`：`{"fields":["字段ID"],"rows":[["值"]]}`。更新已有记录时使用 `feishu-cli bitable record upsert --record-id RECORD_ID`。每批新增最多 500 行；字段值必须与字段 ID 顺序一致。
7. 写入后返回飞书表链接、新增数、更新数、跳过数、发现的历史重复项数和失败批次。不要输出令牌、密钥或用户敏感数据。

## 清理已有重复项

只有用户明确说“清理重复数据”或“删除重复项”时，才可删除飞书记录。先按同一业务键报告删除计划；保留指标最完整的记录，完整度相同则保留采集时间较晚的一条。用户明确授权后，用 `feishu-cli bitable record batch-delete` 分批执行，并复查剩余重复项数。

## 验收标准

- 新手无需填写密钥、Token、App ID、表格 ID。
- 浏览器扩展只采集抖音数据页面，不直接访问飞书。
- 用户只需说“导入飞书”即可触发 Agent 写入已有表。
- 所有外部写入都在用户明确授权后发生。
